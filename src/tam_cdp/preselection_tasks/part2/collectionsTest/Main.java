package tam_cdp.preselection_tasks.part2.collectionsTest;


import java.util.*;

public class Main {
    // Change to what you need.
    static int iteration = 50000;

    static ArrayList<Integer> arrayList = new ArrayList<Integer>();
    static LinkedList<Integer> linkedList = new LinkedList<Integer>();

    static HashSet<Integer> hashSet = new HashSet<Integer>();
    static TreeSet<Integer> treeSet = new TreeSet<Integer>();

    static HashMap<Integer, Integer> hashMap = new HashMap<Integer, Integer>();
    static TreeMap<Integer, Integer> treeMap = new TreeMap<Integer, Integer>();

    public static void main(String[] args) {
/*
        // Compare Lists
        CollectionsTest.compareAddition(arrayList, linkedList, iteration);
        CollectionsTest.compareSearch(arrayList, linkedList, iteration);
        CollectionsTest.compareDeletion(arrayList, linkedList, iteration);

        // Compare Sets
        CollectionsTest.compareAddition(hashSet, treeSet, iteration);
        CollectionsTest.compareSearch(hashSet, treeSet, iteration);
        CollectionsTest.compareDeletion(hashSet, treeSet, iteration);

        // Compare Maps
        CollectionsTest.compareAddition(hashMap, treeMap, iteration);
        CollectionsTest.compareSearch(hashMap, treeMap, iteration);
        CollectionsTest.compareDeletion(hashMap, treeMap, iteration);*/
    }
}
